---
- name: Install Nginx
  hosts: dev
  become: yes 
  connection: ssh 
  gather_facts: false 

  tasks:
  - name: Installing Nginx
    ansible.builtin.yum:
      name: nginx
      state: present 
      update_cache: yes 
  
  # - name: Start Nginx 
  #   ansible.builtin.dnf:
  #     name: nginx
  #     state: started 